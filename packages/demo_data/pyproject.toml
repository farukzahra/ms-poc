[project]
name = "demo-data"
version = "0.1.0"
description = "Fictional enterprise demo dataset for ms-poc"
requires-python = ">=3.12"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["demo_data"]
